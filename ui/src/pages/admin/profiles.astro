---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Breadcrumb from "../../components/core/Breadcrumb.astro";
import DataTable from "../../components/table/DataTable.astro";
import CrudModal from "../../components/forms/CrudModal.astro";
import Toast from "../../components/Toast.astro";
import { getProfiles } from "@/lib/profiles";

// Fetch profiles from API
let profiles: any[] = [];
let errorMessage: string | null = null;

try {
  const apiProfiles = await getProfiles();
  profiles = apiProfiles;
} catch (error) {
  console.error('Failed to fetch profiles:', error);
  errorMessage = error instanceof Error ? error.message : 'Failed to fetch profiles from API';
  // Fallback to empty array to still render the page
  profiles = [];
}

const columns = [
  { key: "code", label: "profile_modal.code", visible: false },
  { key: "name", label: "profile_modal.name", as: "title", subtitleKey: "code", subtitleFormat: "text" },
];
const data = profiles.map((item) => ({
  id: item.code,
  code: item.code,
  name: item.name,
  description: item.description,
  icon: item.icon,
  is_active: item.is_active,
  created_at: item.created_at,
  updated_at: item.updated_at,
}));
const tableFields = [
  { key: "code", label: "profile_modal.code", pk: true, required: true, placeholder: "e.g. ADMIN" },
  { key: "name", label: "profile_modal.name", required: true, placeholder: "e.g. Administration" },
  { key: "icon", label: "profile_modal.icon", placeholder: "Heroicon path" },
  {
    key: "description",
    label: "profile_modal.description",
    type: "textarea" as const,
    placeholder: "Optional details of profile",
  },
];
const fieldKeys = tableFields.map((f) => f.key);
---

<BaseLayout>
  {errorMessage && <Toast message={errorMessage} variant="error" open={true} />}

  <div class="mx-auto w-full max-w-full md:p-0">
    <!-- Breadcrumb Start -->
    <Breadcrumb
      moduleName="Administration"
      pageName="Profiles"
      pageTitle="Profiles"
      homeLink="/"
      moduleNameKey="modules.ADMIN"
      pageNameKey="menu_options.profiles"
      pageTitleKey="menu_options.profiles"
    />
    <!-- Breadcrumb End -->

    <CrudModal
      mode="create"
      modalId="profile-create-modal"
      entityName="Profile"
      fields={tableFields}
      showTrigger={false}
    />
    <CrudModal
      mode="edit"
      modalId="profile-edit-modal"
      entityName="Profile"
      fields={tableFields}
      showTrigger={false}
    />
    <CrudModal
      mode="delete"
      modalId="profile-delete-modal"
      entityName="Profile"
      fields={tableFields}
      showTrigger={false}
    />

    <div
      id="profiles-data"
      data-rows={encodeURIComponent(JSON.stringify(data))}
      data-keys={encodeURIComponent(JSON.stringify(fieldKeys))}
      class="hidden"
      aria-hidden="true"
    >
    </div>

    <script>
      import { initCrudPage } from "@/lib/crudClient";
      import {
        createProfile,
        updateProfile,
        deleteProfile,
      } from "@/lib/profiles";
      import { showToast, flashToast } from "@/lib/toast";

      initCrudPage({
        dataId: "profiles-data",
        editModalId: "profile-edit-modal",
        deleteModalId: "profile-delete-modal",
      });

      // Handle CRUD form submissions
      document.addEventListener("crud-submit", async (e) => {
        const { modalId, mode, data } = e.detail;

        try {
          if (mode === "create") {
            // Create new profile
            await createProfile({
              code: data.code,
              name: data.name,
              description: data.description,
              icon: data.icon,
              is_active: data.is_active !== false,
            });

            flashToast("Profile created successfully!");
            window.location.reload();
          } else if (mode === "edit") {
            // Update existing profile
            await updateProfile(data.id, {
              name: data.name,
              description: data.description,
              icon: data.icon,
              is_active: data.is_active !== false,
            });

            flashToast("Profile updated successfully!");
            window.location.reload();
          } else if (mode === "delete") {
            // Delete profile
            await deleteProfile(data.id);

            flashToast("Profile deleted successfully!");
            window.location.reload();
          }
        } catch (error) {
          console.error("CRUD operation failed:", error);
          showToast(error.message || "An error occurred", "error");
        }
      });
    </script>

    <div class="space-y-5 sm:space-y-6">
      <div class="space-y-5 sm:space-y-6">
        <!-- ====== Table Start -->
        <DataTable
          columns={columns}
          data={data}
          tableId="profiles"
          i18Item="profile"
          detailPage="/admin/privileges"
          mdItem="privilege"
        />
        <!-- ====== Table End -->
      </div>
    </div>
  </div>
</BaseLayout>
