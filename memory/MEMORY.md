# AI Coding Memory — SynapxIA

Running context for AI coding agents. Updated on significant merges to `develop`/`main`/`prod`.
For binding rules see [`../AGENTS.md`](../AGENTS.md) and [`.specify/memory/constitution.md`](../.specify/memory/constitution.md).

---

## What this project is

SynapxIA is a **team collaboration and performance-measurement platform** — a modular
monolith with three surfaces (API, UI, DB) orchestrated by Docker Compose.

Key domain concepts: **Teams → Assignments → Projects / Dimensions → Metrics**.
Users are assigned to teams with roles; teams own projects; dimensions define measurement
axes; metrics record measurements for assignments against dimensions.

---

## Tech snapshot

| Surface | Stack | Port |
|---------|-------|------|
| API | Python ≥3.12, FastAPI + SQLModel, `uv` | 8000 |
| UI | Astro 5 (SSR) + Vite 8 + Tailwind + Flowbite, Bun; Svelte 5 for heavy islands (manual `mount()`) | 4321 |
| DB | PostgreSQL 18, ordered SQL migrations in `db/sql/` | 5432 |

Run everything: `make dev`. Verify: `make test`.

---

## Established patterns (don't reinvent)

### API (backend)
- Domain modules: `api/app/{admin,auth,collab,taxo,genai,inits,insights,workflows}/`
- Shared plumbing (DB session, auth deps) lives in `api/app/internal/` — never duplicated per module.
- List endpoints always take `skip`/`limit`. Logical delete via `is_active=False`.
- Status codes: 409 unique conflict · 400 validation · 403 auth · 404 not found.

### UI (frontend)
- **Types:** 3 interfaces per entity in `ui/src/types/api.ts` → `Entity`, `EntityCreate`, `EntityUpdate`.
- **Services:** CRUD wrappers in `ui/src/lib/{entity}.ts` around `lib/api.ts`.
- **Pages:** `ui/src/pages/{module}/{entity}.astro`. Mirror closest existing page (e.g. `admin/options.astro`).
- **Components:** reuse `DataTable`, `CrudModal`, `Toast` — don't create new alternatives.
- **UI primitives (`components/ui/`):** shared, framework-free building blocks — `FormField.astro` (label + control slot + inline error), `Tabs.astro` + `lib/tabs.ts` `initTabs()` (`data-tab`/`data-tab-panel` toggler; render panels yourself — Astro forbids dynamic slot names), `Button.astro` (primary/secondary/ghost). Shared form styling + validation live in `lib/formClasses.ts` (`inputClass`/`labelClass` — importable by BOTH `.astro` frontmatter and client islands, so islands no longer redefine them — plus `setFieldInvalid`). First adopter: `lib/propose.astro`. Prefer these over re-inlining tabs/field markup.
- **i18n:** every user-facing string in both `ui/src/i18n/en.json` and `es.json`, keyed per entity/module.
- **Layouts:** `BaseLayout.astro` for sidebar pages; `Layout.astro` for standalone full-screen pages (e.g. `/dashboard`, `/login`).
- **`@` alias** resolves to `ui/src/` (via `fileURLToPath` in `astro.config.mjs`).

### DB
- Migrations: ordered SQL files `db/sql/{NN}-{module}-{ddl|insert}.sql`. Additive only.
- Changes only take effect on a fresh volume (`make rebuild`).

---

## Decisions log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-07-14 | **Version history is now READABLE (the "possible follow-up" from the 2026-07-12 versioning entry, shipped): a 5th "Versions" tab on the shared read-only `CatalogDetailModal`, backed by two new read-only endpoints that query the version history `create_version` already writes.** `GET /api/assets/{id}/versions` → `List[AssetVersion]` (new `version_service.list_versions`): the **distinct query** `select(Characterization.version_label, func.min(created_at)).where(asset, is_active).group_by(version_label)` — one row per label including the initial `1.0.0` (which has no `VERSIONING` action), newest-first by date, `is_current = (label == asset.current_version)`, enriched from the asset's `VERSIONING` actions (`content`→new label; `change_type` parsed from `detail`'s trailing `(...)` via `_DETAIL_CHANGE_TYPE` regex; `actor` via one batched `User` lookup) — null enrichment for the initial label is expected. `GET /api/assets/{id}/versions/{version_label}/characterizations` → that label's active char rows (dots in the path param are fine; only slashes break FastAPI paths). **Both gated `require_privilege("LIB","ASSETS",can_edit=False)` — module read only, NO `_ensure_manage`** so gallery viewers read history exactly as they already read the current snapshot; the write-side `POST /{id}/versions` keeps its MANAGE guard. **No DDL** — pure query over `characterizations` + `actions`; `created_at` (not the `VERSIONING` action, which only exists for bumps) is the canonical date source. UI mirrors the History-tab architecture exactly: new `lib/versions.ts` (`getAssetVersions`/`getVersionCharacterizations` + `mountVersions`, cloned from `lib/history.ts`) + `VersionsTimeline.astro` shell, wired by `mountCatalogDetail` alongside `mountRelated`/`mountHistory` (self-mounts on the shared `[data-modal-open]` trigger, tab-agnostic `activateTab()` needs zero change). Each row expands to that version's snapshot rendered by the **Details-tab renderer extracted to an exported `renderCharacterizationSections()` in `catalogDetail.ts`** — so a historical snapshot looks byte-identical to the live Details view and its copy buttons work via the dialog's existing delegated `[data-action="copy"]` handler. Scope: galleries' read-only modal only (prompts/mcps/agents all inherit `CatalogDetailModal`) — the editable `/lib/assets` Svelte dialog is deliberately untouched. | The 2026-07-12 versioning work stored every generation but exposed only the current one ("a version-history browser is a possible follow-up"); the user asked to surface that history as a per-asset Versions tab "with the created at in c[h]aracterization … with a distinct query" and to view each snapshot. Reuses the History-tab self-mount pattern and the existing section renderer rather than inventing a parallel viewer, so the 5th tab is additive and the three catalog detail-modal wrappers need no change. Read gate matches the current-snapshot read path (history is no more sensitive than the current version already shown). |
| 2026-07-13 | **Toolbar buttons collapse to icon-only below `sm`, with a `title`/`data-i18n-title` fallback for the accessible name — new opt-in `FavoritesPill` `collapseLabelOnMobile` prop.** Follow-up to the same-day responsive pass below, scoped to a narrower recurring gap: `CardGallery.astro`'s "★ My favorites"/"+ Propose"/"+ New" buttons and the shared `DataTableToolbar.astro`/`DataTableSearchExport.astro` "+ New X"/"Export" buttons always rendered icon **and** full label, overflowing a 390px row. Fix mirrors the existing `Header.astro` brand-name pattern: `hidden sm:inline` on the label `<span>`, icon stays visible, `title=""` + `data-i18n-title={key}` added for the mobile accessible name. `FavoritesPill.astro` gained `collapseLabelOnMobile?: boolean = false` so its other consumer (`DataTableFilters.astro`'s toggle branch on `/lib/assets`, a 2-per-row mobile grid where the label should stay) is unaffected — only `CardGallery` opts in. Also added defensive `flex-wrap` to `DataTableToolbar.astro`'s inner button row and `CrudModal.astro`'s footer (no visible change today, guards future long labels). | User flagged a screenshot of the Prompt Gallery on mobile showing full-width "★ Mis favoritos" / "+ Proponer" buttons eating the whole row. Reuses the established icon-only-with-title-fallback convention (already used by the toolbar's master-nav link) rather than inventing a new pattern; `collapseLabelOnMobile` follows the same opt-in-prop convention as `favoriteKey`/`manageKey` on DataTable so ~18 unrelated pages stay untouched. |
| 2026-07-13 | **Mobile responsive pass on the header + LIB galleries/repo detail modal: fixed-position notification panel, always-visible search bar, single-row scrollable tabs, wrap-to-two-rows modal header, and a new optional `GalleryCard`/`CatalogDetailModal` `version` prop.** The header search input and its ⌘K badge, the notification bell dropdown, the two detail-modal tab rows (`CatalogDetailModal.astro` + `AssetDetailTabs.svelte`), and the detail modal's title-row all got responsive treatment below `sm`/`lg` rather than being hidden or left to wrap awkwardly. `GalleryCard.astro`'s new `version` prop (mono `vX.Y.Z` chip beside the status pill) is opt-in — future catalog cards should pass `version={item.current_version}` the same way `PromptCard`/`McpCard`/`AgentCard` now do; `CatalogDetailModal.astro` mirrors it in the header, filled by `catalogDetail.ts`. | User reported the search bar was completely missing on phones, the notification dropdown ran off the left edge, the repo/gallery detail modal's tabs wrapped to a second row and its title clamped too early, and the asset's version (shipped in HU-LI09) was never shown in the read-only catalog surfaces. All five were CSS/prop-threading fixes over existing components — no new components, no backend change. |
| 2026-07-12 | **HU-LI08 asset permissions are now ENFORCED (substrate → gate): the repo listing is caller-scoped and every per-asset write requires MANAGE.** `permissions_service` gained the per-user effective-level surface (`assets_user_access`/`accessible_assets`/`user_asset_access`: MANAGE > VIEW over the same scope matching + temporal validity as `assets_user_scopes`) and **`require_asset_manage(session, user, asset_id)` + `AssetAccessForbidden` — the established per-asset write-guard pattern** (superuser bypass first; each routes module wraps it in a tiny `_ensure_manage` → 403 adapter; module RBAC via `require_privilege` stays the OUTER layer). `GET /api/assets/with-access` filters to granted assets BEFORE skip/limit (no-grant = hidden, superusers see all) and returns additive `my_access`; its Postgres-only `array_agg`/`bool_or` aggregate became a Python reduction (SQLite-testable). Guarded: assets PUT/DELETE/versions, characterizations POST/PUT/DELETE, asset_relations writes (source-asset guard), asset_permissions writes (blocks VIEW-user self-escalation). Plain asset create now auto-grants creator USER/MANAGE (mirrors propose). UI: opt-in `manageKey` DataTable prop chain — VIEW rows hide edit/delete/detail-expand. Propose/review/resubmit, galleries and single-asset GET deliberately unchanged. Seed asset 3 (zero grants) is now superuser-only. | User wanted the repo to "show only the assets the person has permissions to" with edit/remove gated by accessibility — and UI-only hiding isn't security, so the writes are enforced server-side. The chosen strict default (no grant = hidden) is safe because both create paths now grant the creator MANAGE. Reuses the existing resolver/scope machinery rather than a parallel ACL implementation; `manageKey` follows the `favoriteKey` opt-in convention so the other ~18 DataTable pages are untouched. |
| 2026-07-12 | **Asset versioning (HU-LI09, versioning half) implements the SAME-ROW model — `assets.current_version` bump + characterization snapshot per `characterizations.version_label` — NOT the docs' "new asset row + EXTENDS relation" wording; WHAT GETS VERSIONED IS THE CHARACTERIZATION SET: saving the repo modal's detail section creates a version (manual Major/Minor/Patch choice → which digit of `X.Y.Z` bumps), while core-field edits save in place via plain PUT, unversioned.** New `api/app/lib/internal/version_service.py` (`bump_label` + one-transaction `create_version`: optional core edits, write the new label's char rows — `values` = FULL desired set, `None` = copy current forward — set `current_version`, insert `VERSIONING`/`FINISHED` action) behind `POST /api/assets/{id}/versions`; `PUT /api/assets/{id}` untouched (and is what the core pencil save uses). `Characterization` now maps `version_label` as a PK field (fixing the model/DB mismatch from the 496980c DDL commit), and EVERY char read/write is current-version-scoped (list filters `coalesce(assets.current_version,'1.0.0')`; the `(asset,feature)`-keyed CRUD resolves the current label — URLs unchanged). `modify_service.resubmit_asset` is version-safe but deliberately does NOT bump (review-loop corrections stay in place). **Known limit:** the galleries' `catalogModal.ts` edit path (PUT + char CRUD) is version-safe but unversioned — follow-up if gallery edits should version too. Deprecation (the other half of HU-LI09) still pending. | The user picked the same-row model (matches the versioning columns the team shipped in DDL commit 496980c; avoids duplicate rows in the repo table) over the docs-literal new-asset-row model, explicitly scoped versioning to the characterization/detail edits ("lo que se versiona es la caracterización; lo demás no se versiona"), and wanted the manual change-type question deciding the semver digit. Old generations stay stored (history, per the HU's "improvements are tracked without losing history") but only the current one is readable via the API today — a version-history browser is a possible follow-up. |
| 2026-07-12 | **Header search bar is now a working command-palette over the sidebar's modules/options, ranked client-side — no new backend/DB.** New pure module `ui/src/lib/searchIndex.ts` scores each `NavOption` against the typed query (`fuzzyScore`: substring match scored by position+coverage, else in-order subsequence fallback), then adds a flat boost for the `LIB` ("AI Library") module and a boost from how many times the user has opened that option (tracked in `localStorage["search_usage_v1"]`, plus a `localStorage["search_recent_v1"]` MRU list for the empty-query "Suggested" list). New Svelte 5 island `SearchPalette.svelte` (mounted manually from `SearchBar.astro`, same pattern as `NotificationBell.svelte`/`NotificationMenu.astro`) renders the dropdown — each row shows a breadcrumb (`Home › {module}`) above the option name, and clicking/Enter both records the usage ping and navigates to `option.path`. Reuses `getNavigationData()`/`readNavCache()` (the same nav_cache the sidebar already fetches) instead of a new endpoint. | User wanted a functioning search (the ⌘K badge was previously decorative) that ranks the AI Library module higher and learns from what a user searches/opens most — explicitly **client-side only, no DB persistence** of search behavior. Reusing the sidebar's already-cached nav data avoids a new API surface entirely; the usage/recency signal is a pure ranking input, not an audit trail, so localStorage is the correct (and requested) home for it. |
| 2026-07-12 | **"My Modifications" removed from the account menu's "My Work"/"My Workspace" section; "Review Requests" ("My Reviews") stays.** `Header.astro`'s `workItems` array now only wires `account_menu.review_requests` → `/lib/review_requests`; the `account_menu.my_modifications` → `/lib/modifications` entry (added 2026-07-08 below) is removed, along with `AccountMenu.astro`'s now-unused `ModificationsIcon` import/ICON entry. The `/lib/modifications` page, `modify_service.resubmit_asset`, and the bell's MODIFICATION → `/lib/modify?action={id}` click-through are unchanged — a proposer with an open MODIFICATION assignment still gets there via the notification bell, just not via a standing menu link. | Product distinction: a proposer's "make the requested changes" task is a one-off, ephemeral follow-up best surfaced via the transient notification bell, whereas a reviewer's open REVIEW queue is durable, recurring work that deserves a stable, always-reachable entry point independent of whether a bell notification is still unread/undismissed. Keeps the "My Work" section from growing menu items that don't need permanent visibility. |
| 2026-07-08 | **Review Requests / My Modifications moved from the LIB sidebar into the profile/account menu, under a new profile-gated "My Work" section.** Same-day follow-up to the decision below. `db/sql/12-admin-insert.sql` no longer seeds `LIB.REVIEW_REQUESTS`/`LIB.MODIFICATIONS` `options`/`privileges` — the pages and their `GET /api/actions/reviews`/`modifications` endpoints are unchanged, only the sidebar registration is gone (already-provisioned DBs need a one-off `UPDATE options SET is_active=false WHERE module='LIB' AND code IN ('REVIEW_REQUESTS','MODIFICATIONS')`). `AccountMenu.astro` gained a `workItems` prop (distinct from the existing static `menuItems`), rendered as a "My Work" section with each item carrying an optional `profiles` allow-list; `Header.astro` wires the two links gated to `ADMINISTRATOR`/`ADMINISTRATIVE`/`REVIEWER`. **New pattern:** `AccountMenu` has no SSR user fetch (unlike the DB-driven sidebar), so profile-gating happens client-side in its existing mount script — after `getUser()` resolves, `[data-profiles]` `<li>`s not matching the cached user's `profile` (or `is_superuser`) get `display:none`, and the whole `[data-work-section]` wrapper hides itself if nothing survives; safe because the `<details>` dropdown starts closed, so there's no flash before the script runs. Reuse this `workItems`/`profiles` pattern for any future "my work" queue instead of adding more sidebar options. | Personal, per-user task queues (as opposed to catalog-browsing entries) fit an account-scoped menu better than the module sidebar, and don't force the sidebar to grow one entry per future review type. The account menu is always visible (no collapse/hide state), unlike the sidebar. |
| 2026-07-08 | **Dismissing a workflow notification is now restricted to informational types (PUBLICATION/REJECTION) only; REVIEW/MODIFICATION get persistent, non-dismissible queue pages instead.** `dismiss_notification` previously let a user dismiss *any* notification by inserting a `workflow_status=FINISHED` row — the same terminal state `review_asset()`/`resubmit_asset()` use to mean "already decided". Dismissing a REVIEW/MODIFICATION notification therefore silently and permanently revoked the assignee's ability to act (their guard queries only match `ASSIGNED`/`NOTIFIED`), with the bell as the *only* way to reach `/lib/review`/`/lib/modify` in the first place. Fix: `actions_service.py` gained `DISMISSIBLE_TYPES = (PUBLICATION, REJECTION)` and `dismiss_notification` now raises `NotificationNotDismissible` (→ 400) for REVIEW/MODIFICATION; `list_notifications`'s collapse-latest-per-thread query was factored into a reusable `_list_open_actions(session, user_id, types)`, exposed as `list_review_requests`/`list_pending_modifications` behind new `GET /api/actions/reviews` / `GET /api/actions/modifications`. Two new sidebar pages, `/lib/review_requests` ("Review Requests") and `/lib/modifications` ("My Modifications"), list each user's open assignments of that type (independent of the bell) and link straight to `/lib/review?action={id}` / `/lib/modify?action={id}`. New `LIB.REVIEW_REQUESTS`/`LIB.MODIFICATIONS` options (seed-only) granted to ADMINISTRATOR/ADMINISTRATIVE/REVIEWER — the same profiles that already hold `can_edit=TRUE` on `LIB.ASSETS` (the only ones who can propose/review/resubmit today). `NotificationBell.svelte`'s dismiss button now only renders for PUBLICATION/REJECTION items. | The bell is a transient activity feed, not a task queue — reviewers had no durable place to find their pending work, and the one escape hatch it offered (dismiss) quietly broke the assignment it was hiding. Reuses the existing `actions` substrate (no schema change) and mirrors the existing "assigned to me" list-page pattern (favorites/assignments) rather than inventing a new UI paradigm. |
| 2026-07-02 | **HU-Modify (resubmit) closes the review loop with one transaction on the same substrate; resubmit flips FEEDBACK→PROPOSED (resolving a doc gap).** New `api/app/lib/internal/modify_service.py` `resubmit_asset(session, proposer, asset_id, data: ModifyRequest)` mirrors `review_service`: it requires an **open MODIFICATION row (ASSIGNED/NOTIFIED) for (asset, proposer.id)** (the modify assignment) **and** that the caller authored the earliest `PROPOSAL` row, plus `asset.status == "FEEDBACK"` (double-submit guard → 409). Then in one commit it updates the asset's editable fields (`name/description/reference/tags/detail` — **not** `category`) + its characterizations in place from `values` (create if a row is missing), inserts `MODIFICATION/FINISHED` (proposer), sets `asset.status = "PROPOSED"`, and inserts `REVIEW/ASSIGNED` for the **original reviewer** (newest REVIEW action's `user_id`). `POST /api/assets/{id}/resubmit` (gated `require_privilege("LIB","ASSETS",can_edit=True)`) maps `ModifyForbidden`→403, `ModifyConflict`→409, `ValueError`→400, `IntegrityError`→409. Frontend: `/lib/modify?action={id}` → `ModifyAction.svelte` (indigo header, read-only reviewer-feedback panel from `action.content`, editable name/description + characterization inputs prefilled from existing char values — built from `getSpecificationsbyCategory`, rich features as textareas, required specs enforced; `markNotified(id)` on open); the bell's MODIFICATION branch routes here (was a "coming soon" toast). **Doc-gap call:** `lib-status.md`/HU-LI14 never state the resubmit status and there is no `MODIFIED`/`IN_REVIEW` enum value — it **must** be `PROPOSED` because `review_service` guards `status == "PROPOSED"`, so re-review would otherwise 409. The cycle is uncapped (matches the docs). | Completes the propose→review→modify→re-review workflow the user asked for ("the user in charge of the proposal … check the feedback … make the modifications … resubmit … send it to review again"). Reuses the `actions` substrate — every transition is a new row — so no schema/DDL change (enums already seeded, REVIEWER profile from PR #69). Editing is limited to name/description/characterizations (HU-LI14 Data = `assets` + `characterizations`); category is fixed because it drives the spec/characterization set. Same reviewer per the docs' definite article ("the reviewer"). |
| 2026-07-02 | **HU-Review reuses the `actions` substrate — the reviewer's decision is one transaction of new rows, never an update; Propose redirects back to the originating gallery.** New `api/app/lib/internal/review_service.py` `review_asset(session, reviewer, asset_id, decision, feedback)` mirrors `propose_service.propose_asset`: it requires an **active REVIEW row (ASSIGNED/NOTIFIED) for (asset, reviewer.id)** — the assignment — plus `_is_eligible(reviewer)` (reuses propose's REVIEWER_PROFILES + superuser), and `asset.status == "PROPOSED"` (double-submit guard → 409). Then in one commit it inserts `REVIEW/FINISHED` (reviewer.id), sets `asset.status` (`DECISIONS = {approve:(PUBLISHED,PUBLICATION), reject:(REJECTED,REJECTION), changes:(FEEDBACK,MODIFICATION)}`), and inserts the **proposer** action (proposer = author of the asset's earliest `PROPOSAL` row) `<PUBLICATION|REJECTION|MODIFICATION>/ASSIGNED` with the feedback in `content`. `POST /api/assets/{id}/review` (gated `require_privilege("LIB","ASSETS",can_edit=True)`) maps `ReviewForbidden`→403, `ReviewConflict`→409, `ValueError`→400, `IntegrityError`→409. Frontend: `/lib/review?action={id}` → `ReviewAction.svelte` (mirrors `ShowAction`, amber header, char list, feedback textarea, Approve/Request-changes/Reject; feedback required for reject+changes; `markNotified(id)` on open); the bell's REVIEW branch routes here. **Propose→gallery redirect:** on a successful propose that came from a gallery CTA (`lockedCategory` set), `propose.astro` does `window.location.href = "/lib/" + lockedCategory.toLowerCase() + "?proposed=1"`; `catalogGallery.ts` `initCardGallery` shows the `gallery.proposed` toast when `?proposed` is present and strips it via `history.replaceState`. | User wanted the REVIEW notification to route to a real review page (approve/reject/feedback), and a proposer returning to the gallery they proposed from with a confirmation. The `actions` table already had every enum (ACTION_TYPE PUBLICATION/REJECTION/MODIFICATION + WORKFLOW_STATUS) and the read-sides (history/notifications/Show Action) already localize these types + render `content`, so no schema/DDL change — just the write-side transaction + a frontend page. **HU-Modify** (proposer editing after `changes`→FEEDBACK/MODIFICATION) shipped in the same PR — see the entry above. **Env caveat (again):** the container's default venv (Py 3.14) can't import the backend (pydantic `eval_type_backport` `AssertionError`); run pytest on a **Python 3.12 venv** (`uv venv --python 3.12` + `uv pip install -r requirements.txt pytest httpx`). |
| 2026-07-02 | **PgAdmin is published on host port `8090` by default (not 8080), via a new `PGADMIN_HOST_PORT` env var.** `docker-compose.yml` maps `${PGADMIN_HOST_PORT:-8090}:80` — only the **host** side moved; the container port stays 80. Same pattern as `DB_HOST_PORT` (2026-07-01). Browser URL is now `http://localhost:8090`; `.env.template` documents the knob. | Host 8080 clashed with other projects ("port is already allocated"). Host-only remap is zero-risk (nothing internal changes) and per-machine overridable. |
| 2026-07-02 | **`specifications` gained a per-(category,feature) `required` flag; the Propose form is a two-step wizard.** Added `required BOOLEAN NOT NULL DEFAULT FALSE` to the `specifications` table (`db/sql/21-taxo-ddl.sql` + seed `22-taxo-insert.sql`), exposed on `SpecificationBase`/`Create`/`Update` (`api/app/taxo/internal/models.py`) and the `Specification` UI type. `/lib/propose` now: (1) is a **two-step wizard** when the type has specs — Details button "Caracterizar" (`propose.characterize`) validates name/category + advances to the Characterization tab; Characterization button "Proponer para revisión" (`propose.submit`) posts. Label is tab-driven via `updateSubmitLabel()`/`hasCharStep()`/`onDetails()`; the old `charVisited` nudge is gone. (2) **enforces required characterization fields from the spec table** — required specs get a red `*` (a sibling `<span>`, NOT inside the `[data-i18n]` label, so `loadClientTranslations` won't wipe it) + `data-required="1"`; step-2 submit blocks (via `setFieldInvalid(el, null, true)` + toast `propose.characterization_required`) until each is filled; optional specs may stay blank. Required-ness is per-(category,feature) because a feature can be required for one type and optional for another (e.g. AGENTS: INSTRUCTIONS required, TOOLS optional). | User wanted characterization to be a required *step* AND individual fields required/optional "from the specification table" — but no such flag existed (specs were only category/feature/default_value). Adding it on `specifications` (not `features`) makes it per-category, matching the requirement. **Gotcha:** don't set the `*` marker as a child of the `[data-i18n]` feature label — the i18n runtime sets the label's `textContent` on language switch and would delete the star; put the feature name in its own `[data-i18n]` span and the star beside it. **Gotcha:** the container's Python 3.14 venv can't import the backend (pydantic `eval_type_backport` `AssertionError`); run backend pytest on a 3.12 venv. **Deploy:** additive column → local `make rebuild`; Neon needs a one-off `ALTER TABLE specifications ADD COLUMN required …`. |
| 2026-07-01 | **Svelte 5 adopted for heavy interactive islands — via manual `mount()`, NOT the `@astrojs/svelte` integration.** UI stays Astro 5 (SSR) + Vite 8; added `svelte@5` + raw `@sveltejs/vite-plugin-svelte` (registered in `astro.config.mjs` `vite.plugins`) with `vite` pinned via `overrides` to `^8`. Svelte components live in `ui/src/components/svelte/`, are mounted manually with Svelte's `mount()` in a bundled `<script>`, and reuse the existing `lib/*` services + `translate()` i18n. Islands so far: `ShowAction.svelte` (`/lib/show-action`) + `NotificationBell.svelte` (header bell, mounted from `NotificationMenu.astro` via a `display:contents` wrapper; `lib/notifications.ts` trimmed to just its data-layer services) + `Foro.svelte` (asset discussion HU-LI06, self-mounted from `Foro.astro`, replacing the ~320-line `mountForo` vanilla controller — `lib/foro.ts` shrank 452→98 lines to pure services + `groupDiscussion`/`summarizeDiscussionCounts` helpers; `catalogDetail.ts` no longer calls `mountForo`, the island self-hooks the shared `[data-modal-open]` trigger) + `AssetDetailTabs.svelte` (editable Characterizations/Related/Permissions tabs on `/lib/assets`, mounted from `AssetDetailModal.astro`, replacing the 944-line `lib/assetDetailTabs.ts` **deleted**). Tailwind content glob extended to `.svelte`. **Two migration patterns, by boundary type:** (1) *self-contained widget with a `mount*(cfg)` boundary* (foro/bell) → move it to `components/svelte/X.svelte`, self-mount from its `.astro` shell (query `[data-*-root]` + read the id from a `data-` attr), leave the shared `lib/*` services in place, drop the old `mount*` call from the caller. (2) *controller with an imperative API a parent orchestrates* (assetDetailTabs: parent calls `tabs.hydrate/flush/reset/…`) → expose the same methods as `export function`s in the component; Svelte 5 `mount()` returns them, so `const tabs = mount(X, {target, props})` gives the parent the exact same controller object — only the construction line changes. Keep data-risky logic (e.g. diff-based `flush`) byte-identical; read reactive `$state` instead of scraping the DOM. Svelte template comments are `<!-- -->`, NOT JSX `{/* */}` (the latter is an "Unexpected token" parse error); seed `$state` from a prop's initial value via `untrack(() => prop)` to avoid the `state_referenced_locally` warning. **catalogGallery is NOT a good Svelte target** — it's a behavior controller over Astro-SSR'd `[data-card]` DOM (wires filter/search/vote, doesn't render cards); a faithful port either drops per-request SSR (full CSR) or just relocates vanilla into `onMount`. Left vanilla. | The `@astrojs/svelte` integration's `client:*` island codegen mis-compiles the generated `astro-entry:*.svelte` (`export { default } from …` parsed as Svelte → "Unexpected token") whenever a Svelte island shares a page with vanilla `<script>`s — which every `BaseLayout` page has. Our registry caps `@astrojs/svelte` at `9.0.0` (the buggy version) and it fails on both Vite 7 and 8. Manual `mount()` bypasses the island mechanism entirely and builds clean on Vite 8. **Gotcha:** do NOT `bun update --latest` — it pulls Astro 7 (Rolldown, RC-grade) + Tailwind 4 + Vercel 11 and still can't fix Svelte (integration stuck at 9.0.0). Stay on Astro 5.x. |
| 2026-07-01 | **Postgres is published on host port `5442` by default (not 5432), via a new `DB_HOST_PORT` env var.** `docker-compose.yml` maps `${DB_HOST_PORT:-5442}:5432` — only the **host** side moved; the **container port stays 5432**, so the API + PgAdmin still reach it in-network as `db:5432` and `DB_PORT` (the internal connection port in `api/app/core/config.py`) is unchanged. Host clients (psql/DBeaver/a locally-run API) use `localhost:5442`. `.env.template` documents both (`DB_PORT`=in-network 5432, `DB_HOST_PORT`=host 5442). | The stack failed to start when another project's Postgres container (or a local Postgres) already held host 5432 ("port is already allocated"). Remapping only the host side is a zero-risk config change (no connection strings touched) and `DB_HOST_PORT` lets each machine pick a free port. **Gotcha:** don't bump `DB_PORT` to fix host clashes — that's the container-internal port; changing it breaks the API↔DB and PgAdmin connections. Move `DB_HOST_PORT` instead. |
| 2026-06-30 | **New `REVIEWER` profile + the Propose form is type-aware (locked type, per-type title, specs-driven characterization).** Added a fourth profile `REVIEWER` ("Asset Reviewer") to the canonical seed `db/sql/12-admin-insert.sql` (with LIB full-edit + TAXO read privileges; applied on a fresh volume / `make rebuild`). `propose_service.list_reviewers`/`resolve_reviewer` now accept **`ADMINISTRATOR`/`ADMINISTRATIVE`/`REVIEWER` or any superuser** (`REVIEWER_PROFILES` tuple + `_is_eligible`), not the old `ADMINISTRATIVE`-only filter. The `/lib/propose` page locks the asset type when reached via `?category=` (read-only field, no select), sets a per-type heading (`propose.title_{prompts,mcps,agents}`), and renders a Characterization section client-side from `getSpecificationsbyCategory` (one `name="<FEATURE_CODE>"` field per spec, prefilled with `default_value`, textarea for rich features), submitted as `ProposeRequest.values`. | The reviewer dropdown was empty: the seed admin is `ADMINISTRATOR` but the filter required `ADMINISTRATIVE`. Rather than only widen to existing admins, the team wanted a dedicated reviewer role going forward (user: "check if there is a reviewer profile or role … and add it"). The propose backend already took the category as-is + `values` + `reviewer_id`, so the type-aware form was mostly UI. **Gotcha:** the `.astro` client island can't read the frontmatter `inputClass`/`labelClass` consts — they're redefined inside the bundled `<script>`. |
| 2026-06-27 | **Standardized API response/error envelope.** Every JSON response under `/api/` except `/api/auth/*` (and `/health`, `/`, docs) is wrapped as `{data, error, meta}` by a global middleware + `HTTPException`/`RequestValidationError` handlers in `api/app/main.py` (helpers in `api/app/internal/responses.py`); routes still return bare models / raise `HTTPException` (no per-endpoint edits). `meta` echoes skip/limit + list count (no grand total -- deferred). The UI unwraps `.data` centrally in `ui/src/lib/api.ts` (`unwrapEnvelope`), so services/pages are unchanged. Auth excluded because fastapi-users + the bespoke fetches in `ui/src/lib/auth.ts` read raw `{access_token}`/`UserRead`/`{detail}`. | One predictable shape for the data layer. Done atomically in-place (not versioned per Constitution II) because the bundled UI is the only consumer -- coordinated wrap+unwrap in one PR. Gotcha: OpenAPI still documents the inner model (wrapping is post-serialization), so `/docs` shows the un-enveloped shape -- per-route envelope docs deferred. |
| 2026-06-26 | **Shared `FavoritesPill`; DataTable filter slots accept an `aria-pressed` `<button>`; curated galleries are published-only with a Propose CTA.** The "★ favorites" filter is now **one** component — `components/lib/FavoritesPill.astro`, an `aria-pressed` toggle whose active styling comes from Tailwind's built-in **`aria-pressed:` variant** (no JS class-juggling) — reused by both `CardGallery` and the assets `DataTableFilters` (`asToggle` branch, replacing the switch). This extends the 2026-06-25 "element-type-aware slots": `advancedTable.ts` `slotValue`/`setSlotValue` now also read/clear a button's `aria-pressed`, and a new `bindFilterControl(el, param)` binds **`click`** for toggle buttons / **`change`** for selects+checkboxes, and on a header-funnel `<select>` change closes its `<details>` (plus a document outside-click closer). Galleries `/lib/{prompts,mcps,agents}` are **published-only** (filter rows to `PUBLISHED`, pass `statuses={[]}` so `CardGallery` hides the status `<select>`) and carry a primary **Propose** CTA → `/lib/propose?category=<CODE>` (propose page preselects from `?category=`). The toolbar **Reset** button moved to the left group + went discreet (borderless/muted). | One favorites control everywhere (user: "reuse the favorites button … in all pages"); galleries are browse-only published surfaces but still need a contribution entry point. **Gotcha (recorded):** `DataTable.astro` must run `initAdvancedTable` **once** — via the bundled JSON-config `<script>`. A leftover literal-`/src/...` `type="module"` import double-bound every handler in dev (Export toggle fired twice → menu never opened) and 404'd in the Vercel build; it was deleted. UI-only, no backend/DDL. |
| 2026-06-26 | **LIB curated catalogs (Prompt/MCP/Agent) are view-only; their detail modal is tabbed.** The shared read-only `CatalogDetailModal` body is split into **Details / Discussion / Activity** tabs (`data-detail-tab`/`data-detail-panel`, wired in `catalogDetail.ts`; Related sits under Details). The cards (`PromptCard`/`McpCard`; `AgentCard` already had none) and the modal footer drop **Edit/Delete**, and the gallery pages drop the **New** button + create/edit `FormModal` + delete `CrudModal`. Editing/removing assets is now **only** on `/lib/assets` (DataTable + `AssetDetailModal`) and creation via the Propose flow. The foro discussion is minimalist: composer at the bottom, per-question **Answer** is a link that reveals an inline box. On `/lib/assets` the row **detail** button now opens `AssetDetailModal` with `data-asset-section="detail"` (its Chars/Related/Permissions tabs) instead of an inline expanded `<tr>`. | User wanted the specific-catalog pages to be browse-only reference surfaces with a cleaner, tabbed detail view, and the assets-repo detail to open as a modal (consistent with the catalog detail) rather than collapsing on the row. Reuses the existing `assetDetailTabs` modal mode + all `data-foro-*` hooks — UI-only, no backend/DDL. |
| 2026-06-25 | **DataTable column filters can render inline in a column header (funnel) or as a toggle** — opt-in `filterNHeaderColumn` / `filterNAsToggle` props on `DataTable.astro`; slots are built once via `buildFilterConfigs` (`lib/datatable.ts`) and shared by `DataTableHead` (funnel `<details>` hosting the same `<select>` id/`data-column-key`) and `DataTableFilters` (toolbar). `advancedTable.ts` binds by id so placement is free, and is now element-type-aware (reads a `<select>` value OR a checkbox's `data-on-value`). Also surfaced **per-user asset permission scopes**: `permissions_service.assets_user_scopes` + a `permission_scopes` field on `/api/assets/with-access` feed the assets "Privileges/Permisos" membership filter (USER/ROLE/TEAM/UNIT/PROJECT/PUBLIC, matched against the user's unit + active collab assignments/projects). First adopter: `/lib/assets` (Category/Status → header funnels, Favorites → toggle, Privileges → toolbar). | Declutter the asset toolbar and put per-column filters on their columns, reusably (DataTable is shared by ~18 pages — all unchanged when the new props are unset). The permissions filter answers "what can I access?" by scope, not access-level. Note: `/with-access` uses Postgres-only `array_agg`/`bool_or`, so its query is Postgres-verified while the scope logic is unit-tested on SQLite. |
| 2026-06-24 | **Don't share the API virtualenv between host and container.** `docker-compose.yml` bind-mounts `./api:/app`; an anonymous `- /app/.venv` volume now keeps the image-built venv (correct arch) from being shadowed by the host's `./api/.venv`. Also fixed `make pytest` to run `uv run pytest` (the venv bin isn't on the container `PATH`). Python stays pinned at **3.14** per team direction. | The API was crashing on boot with `ModuleNotFoundError: No module named 'pydantic_core._pydantic_core'`. Root cause was **not** a missing wheel — it was a stale **x86_64** host `./api/.venv` (built earlier under emulation) bind-mounted over the container's correct **aarch64** venv (`SOABI cpython-314-aarch64-linux-gnu`), so the arch-tagged `.so` didn't match. cp314 arm64 wheels exist and the image builds a working venv. |
| 2026-06-24 | **Asset interactions ride the generic `actions` table — never a new table.** Votes = `actions` `type=VOTE`, `content` POSITIVE/NEGATIVE, one active row per (user, asset); comments/questions/answers (future) = `type` COMMENT/QUESTION/ANSWER threaded via `parent`; history = read over `actions`; notifications = `actions` where `workflow_status` ASSIGNED/NOTIFIED. Backend logic centralizes in **`api/app/lib/internal/actions_service.py`** (Asset Action Service); frontend in `ui/src/lib/actions.ts` + a vote bar on the shared `GalleryCard`/`CatalogDetailModal`. Also mapped the existing `workflow_status` DDL column onto the `Action` model (additive, was missing). | All `ACTION_TYPE` values are already seeded; the `actions` table has every needed column (`type`/`workflow_status`/`content`/`parent`). One substrate + one service avoids per-feature tables/duplication and is the foundation for the lib roadmap (foro/history/notifications). Per the project user-story spreadsheet this is HU-LI05 "Vote"; `docs/user-stories/04-lib.md` numbers it HU-LI07 (different numbering, left uncorrected). |
| 2026-06-18 | Characterization **`value` vs `detail`**: short fields (PLATFORM/MODE/MODEL/TEMP/OVERVIEW) keep their payload in `value`; **rich** fields (PROMPT_TEMPLATE/EXAMPLE_OUTPUT/TOOLS/CONTENT/SERVER_CONFIG/INSTRUCTIONS) keep it in `detail` (with `value` as a short summary), matching the seed. `mountCatalogModal` `features` accept `{ name, column }`; cards/detail-view read the right column. | Reading `value` everywhere showed summaries instead of the real content (e.g. MCP tool chips rendered the sentence "List of tools…"). |
| 2026-06-18 | Reusable **read-only "big card" detail view**: `gallery/CatalogDetailModal.astro` + `lib/catalogDetail.ts` `mountCatalogDetail({ modalId, editModalId, sections })`. Card click opens it via the card's **`data-detail-modal`** (GalleryCard `detailModalId`); `catalogGallery.ts` prefers it over the edit modal. Sections are `inline`/`block`/`code`(+Copy)/`tools`. Also: `GalleryCard` opt-in **`hero`** gradient header (`heroClass`), `CatalogFormModal` `hideDetailsHeading` (grouped forms), `CardGallery` grid widened to `auto-fill minmax(360px,1fr)`. | User wanted to click a card and read full descriptions/instructions "like a big picture"; built once, used by Prompt/MCP/Agent. **HU-LI15–19 recipe addition:** add `{X}DetailModal.astro` (wrap `CatalogDetailModal` + `mountCatalogDetail` with that category's sections) and set the card's `detailModalId`. |
| 2026-06-17 | LIB **curated catalogs** (Prompt Gallery first) are final-user **card galleries**, not admin `DataTable` pages. Reusable foundation: `components/lib/gallery/{CardGallery,GalleryCard,CatalogFormModal}.astro` + `lib/{catalogGallery,catalogModal}.ts`. The page uses a **bundled `@/` `<script>`**, never inline `type="module"` + `/src/...` imports (those 404 in the Vercel build). | Catalogs are discovery/reuse surfaces for devs/tech users; one card framework, per-catalog custom card+modal rendering. **Extension recipe** for HU-LI13–19 (MCP/Agent/Flow/Skill/Assistant/RAG/Model): add `{X}Card.astro` (wrap `GalleryCard` + custom chips), `{X}FormModal.astro` (wrap `CatalogFormModal` + that category's feature inputs + `mountCatalogModal({category, features})`), a `lib/{x}.astro` page, and i18n keys. No backend work — all categories+specs are seeded. |
| 2026-06-15 | `DataTable.astro` split into an orchestrator shell + `components/table/partials/` (Toolbar, Filters, SearchExport, Head, Body, Cell, Actions, Pagination, Empty) + pure helpers in `lib/datatable.ts` + types in `types/datatable.ts` | The 567-line monolith was a "god component". Split is **DOM-identical** (verified by before/after dev-render diff), so `advancedTable.ts` (which binds by `${tableId}-*` ids + DOM structure) and the card-view CSS are untouched. Sub-parts take `tableId`/props and rebuild the same ids. The 3 filter selects are now one loop. |
| 2026-06-15 | `DataTable` column config gained parametrized field types via `as` (`title`/`status`/`tags`/`date`/`badge`/`boolean`/`text`) + per-page `subtitleKey`/`subtitleFormat` | One reusable config drives both the desktop table and the mobile card; pages only annotate their `columns`. `title` renders bold name + muted subtitle (`.dt-title`/`.dt-subtitle`); the subtitle's source column is set `visible:false` so it isn't shown twice (still in `data` for export/filter). Card view keys off `td[data-col="title"]`, not column order. `advancedTable.ts` untouched. |
| 2026-06-07 | Added `ui/src/layouts/Layout.astro` (minimal shell) | `dashboard.astro` used a standalone full-screen design, not the sidebar BaseLayout; the file was simply missing. |
| 2026-06-07 | Hardened `@` alias to `fileURLToPath(new URL('./src', import.meta.url))` | Literal `/src` is filesystem-absolute and breaks Vercel builds. |
| 2026-06-07 | Created `AGENTS.md` per project + root, thin `CLAUDE.md` pointers | AI tools need a curated single entry point; Constitution and patterns were undiscoverable. |
| 2026-05-XX | `catalog` module renamed to `taxo` | Better reflects digital asset taxonomy purpose. |
| 2026-04-XX | `roles` renamed to `profiles` in admin | Profiles better represents the concept; roles concept retained for collab assignments. |
| 2026-04-XX | Master-detail pattern for dimensions → metrics | Mirrors admin roles → privileges pattern for consistency. |

---

## Feature status

### Shipped
| Feature | Domain | Status |
|---------|--------|--------|
| Admin CRUD (users, profiles, modules, options, privileges, lists, list_items, item_translations) | admin | ✅ done |
| Taxonomy (categories hierarchical, features) | taxo | ✅ done |
| Team collaboration (teams, roles, assignments, projects, dimensions, metrics) | collab | ✅ done |
| Asset library (assets, characterizations, favorites, actions, asset_relations) | lib | ✅ done |
| Asset voting (HU-LI05) — `actions` `type=VOTE` (POSITIVE/NEGATIVE toggle) + **Asset Action Service** | lib | ✅ done |
| Asset foro (HU-LI06) — comments/questions/answers as `actions` (COMMENT/QUESTION/ANSWER, answers threaded via `parent`); discussion section on the gallery detail modal | lib | ✅ done |
| Related assets (HU-LI07) — read-only "Related" section on the gallery detail modal (both directions, de-duped) over the existing `related_assets` table; reverse + resolved routes (`/target/{id}`, `/related/{id}`) | lib | ✅ done |
| Asset history (HU-LI10) — read-only activity timeline on the gallery detail modal (creation marker + votes/comments/Q&A/workflow actions, newest first) over `actions`; `get_asset_history` + `GET /api/actions/history/asset/{id}` | lib | ✅ done |
| Workflow notifications (HU-LI11) — header bell lists the user's open assignments (REVIEW/MODIFICATION/PUBLICATION/REJECTION) as latest-status `actions` threads (ASSIGNED bold → NOTIFIED → FINISHED gone); `list_notifications`/`mark_notified`/`dismiss_notification` + `/api/actions/notifications*`. **Click-through wired** (2026-07-01): PUBLICATION/REJECTION → Show Action; REVIEW/MODIFICATION → Review/Modify pages. **Dismiss restricted** (2026-07-08) to PUBLICATION/REJECTION only — REVIEW/MODIFICATION are no longer dismissible from the bell (see decisions log). | lib | ✅ done |
| Show Action (HU-Show Action) — read-only outcome view for PUBLICATION/REJECTION notifications (`/lib/show-action?action={id}`): branded card (emerald/check vs red/x), asset name + message + timestamp; marks seen on open; Back + Dismiss(→FINISHED). Frontend-only, reuses `GET /api/actions/{id}` + `GET /api/assets/{id}`. | lib | ✅ done |
| Review Requests / My Modifications (2026-07-08) — persistent pages (`/lib/review_requests`, `/lib/modifications`) listing a user's open REVIEW / MODIFICATION assignments, independent of the (now non-dismissible) notification bell; `list_review_requests`/`list_pending_modifications` + `GET /api/actions/reviews` / `GET /api/actions/modifications`. **Entry point is the profile/account menu's "My Work" section** (moved off the LIB sidebar same-day), gated client-side to ADMINISTRATOR/ADMINISTRATIVE/REVIEWER. | lib | ✅ done |
| Propose an asset (HU-Propose) — review-workflow entry point: transactional `propose_asset` inserts asset (PROPOSED) + characterizations (from category specs) + PROPOSAL/FINISHED + REVIEW/ASSIGNED (reviewer) + MANAGE permissions; `POST /api/assets/propose` + `GET /api/assets/reviewers` + `/lib/propose` page. **This generates the REVIEW assignment that HU-LI11 notifications surface.** On success from a gallery CTA it redirects back to `/lib/{category}?proposed=1` (gallery toast). | lib | ✅ done |
| Review an asset (HU-Review) — reviewer's decision step: `review_service.review_asset` (transactional) closes the reviewer's REVIEW/FINISHED, flips asset status (approve→PUBLISHED / reject→REJECTED / changes→FEEDBACK), and notifies the proposer via a new PUBLICATION/REJECTION/MODIFICATION action (ASSIGNED, feedback in `content`); `POST /api/assets/{id}/review` + `/lib/review?action={id}` page (`ReviewAction.svelte`, opened from the bell's REVIEW notification, marks it NOTIFIED on open). 403 non-assigned/ineligible, 409 non-PROPOSED. | lib | ✅ done |
| Modify an asset (HU-Modify) — proposer's resubmit step after "request changes": `modify_service.resubmit_asset` (transactional) updates the asset + characterizations in place, closes the proposer's MODIFICATION/FINISHED, flips status **FEEDBACK→PROPOSED**, and re-arms the **same original reviewer** with a new REVIEW/ASSIGNED; `POST /api/assets/{id}/resubmit` + `/lib/modify?action={id}` page (`ModifyAction.svelte`, opened from the bell's MODIFICATION notification, marks it NOTIFIED on open; shows the reviewer feedback + editable name/description/characterizations). 403 non-proposer/no-open-MODIFICATION, 409 non-FEEDBACK. Completes the propose→review→modify→re-review loop. | lib | ✅ done |
| Prompt Gallery (HU-LI12) — final-user card gallery on the reusable gallery framework | lib | ✅ done |
| MCP Directory (HU-LI13) — second card catalog (Mode chip, tool chips, Copy-config) | lib | ✅ done |
| Agent Index (HU-LI14) — third catalog; first **hero card** + uses the reusable big detail view | lib | ✅ done |
| Curated catalogs HU-LI15–19 (Flow/Skill/Assistant/RAG/Model) — framework ready, pages pending | lib | ⬜ pending (use the gallery extension recipe) |
| Asset versioning (HU-LI09, versioning half) — every repo edit-save creates a new version on the same asset row: manual Major/Minor/Patch choice bumps `current_version`, characterizations snapshotted per `version_label` (old generations kept as history), VERSIONING action in the timeline; `version_service` + `POST /api/assets/{id}/versions` + change-type picker/version badge/Version column in the UI | lib | ✅ done (deprecation half ⬜ pending; gallery-edit versioning = follow-up) |
| Version history browser (HU-LI09 follow-up) — read-only "Versions" tab on the shared gallery detail modal listing every version newest-first (date/actor/change-type/current badge) with an expandable per-version characterization snapshot; `version_service.list_versions`/`get_version_characterizations` + `GET /api/assets/{id}/versions` + `GET /api/assets/{id}/versions/{version_label}/characterizations` (read-gated), `lib/versions.ts` + `VersionsTimeline.astro` | lib | ✅ done |
| Header search palette — command-palette search over sidebar modules/options, ranked by text relevance + AI Library (LIB) module boost + client-side usage/recency (`lib/searchIndex.ts`, `SearchPalette.svelte`); ⌘K/Ctrl+K to open, breadcrumb per result, click/Enter navigates | core/ui | ✅ done |
| Auth (JWT HS256 + bcrypt, /me, register, change-password) | auth | ✅ done |
| Vercel deployment (API serverless via Mangum + UI static + Neon DB) | infra | ✅ done |
| Per-service CLAUDE.md (api/ui/db ~800 lines each) | docs | ✅ done |
| Changelog HH:MM timestamps + hook automation | tooling | ✅ done |

### In progress / stubs
| Domain | Status | Notes |
|--------|--------|-------|
| genai | ⚠️ stub | No models or routes yet — needs SpecKit spec first |
| inits | ⚠️ stub | No models or routes yet |
| insights | ⚠️ stub | No models or routes yet |
| workflows | ⚠️ stub | No models or routes yet |

### Next (P1.1 in progress)
| Item | Status |
|------|--------|
| API linting: ruff + black + isort + mypy | ✅ config added to pyproject.toml |
| UI linting: ESLint (Astro + TS) | ✅ .eslintrc.json + package.json updated |
| pytest skeleton (api/tests/) | ✅ conftest + health/auth/users test files |
| Makefile: make lint/fmt/fmt-check/pytest/lint-ui | ✅ updated |
| memory.md expansion | ✅ this update |

---

## Known blockers

| Severity | Issue | Notes |
|----------|-------|-------|
| P0 | Vercel build-time API fetch | Static pages call `localhost:8000` during `astro build`; fails on Vercel (`ECONNREFUSED`). Fix: set `PUBLIC_API_BASE_URL` env in Vercel project settings pointing to deployed API. `api.ts` returns `[]` on failure so pages pre-render empty and hydrate client-side. |
| P1 | API linting not CI-gated | `make lint`/`make fmt-check` now exist but not wired into GitHub Actions. Violations won't block PRs until CI workflow is added. |
| P1 | No UI tests | Manual verification per quickstart guides only. ESLint now wired; UI test framework (Playwright) is post-launch scope. |
| P1 | caniuse-lite stale | Run `npx update-browserslist-db@latest` in `ui/` before next UI release. |
| P1 | DataTable list pages truncate at 100 rows | ~18 admin/collab/taxo pages fetch their main list with the default `limit=100` (`getX()` with no args), then `DataTable`/`advancedTable` do **all** filter/paginate/search/export **client-side** over only those 100 rows. Tables >100 rows silently lose data and "Showing X of Y" lies. First hit: `list_items` (203 rows) showed 2 of 5 `GENAI_DEV_ADOPTION/en` values — patched ad-hoc with `getListItems(0,1000)` in `ui/src/pages/admin/list_items.astro`. `/select` endpoints are unbounded, so dropdowns are safe. **Proposed transversal fix (not yet done):** _Nivel 1_ — add `apiGetAll<T>(route, pageSize=200)` to `ui/src/lib/api.ts` that auto-paginates `skip/limit` until exhausted (cap ~10k), expose `getAllX()` per `lib/{entity}.ts`, migrate the list pages; keeps client UX + bounded per-request pagination. _Nivel 2_ (only for tables that grow: metrics/assets/actions) — server-side filter via query param (`/api/metrics?dimension=X`; `list_items` already has `/list/{code}`). _Nivel 3_ — server-driven `DataTable` (fetch on filter/page change); big refactor, defer until volume warrants. |
| P2 | Stub domains not implemented | `genai`, `inits`, `insights`, `workflows` are empty placeholders — each needs a SpecKit spec before implementation. |
| P2 | No Alembic migrations | Append-only SQL files work for greenfield; first breaking schema change in production will require `api/alembic/` setup. |

---

## Docs routing table

| Task | Primary doc | Supporting |
|------|-------------|------------|
| Onboarding / what is this | `README.md` | `memory/memory.md` (this file) |
| User stories (by module) | `docs/user-stories/README.md` | `docs/diagrams/`, source `Control H de U.xlsx` |
| API changes | `api/CLAUDE.md` | `AGENTS.md` |
| UI changes | `ui/CLAUDE.md` | `AGENTS.md` |
| DB / schema changes | `db/CLAUDE.md` | `AGENTS.md` |
| Auth deep-dive | `api/AUTH.md` | `api/CLAUDE.md` § Auth |
| Adding a resource (full stack) | `api/CLAUDE.md` § "Adding a new resource" | `ui/CLAUDE.md` § "Adding a CRUD page", `db/CLAUDE.md` § "Adding a new table" |
| Adding a whole new domain | `api/CLAUDE.md` § "Adding a new domain" | `db/CLAUDE.md` § "Module bands" |
| Deployment to Vercel + Neon | `docs/DEPLOYMENT.md` | `api/CLAUDE.md` § Deployment |
| Constitution rules | `.specify/memory/constitution.md` | `AGENTS.md` |
| SpecKit workflow (spec → plan → implement) | `AGENTS.md` § SpecKit | `specs/` + `.specify/templates/` |
| Changelog discipline | `AGENTS.md` § Changelog | `.claude/hooks/update-changelog.sh` |
| Linting + tests | `api/CLAUDE.md` § "Linting & testing" | `api/pyproject.toml` `[tool.*]` sections |
| Troubleshooting | `api/CLAUDE.md` § Debugging | `ui/CLAUDE.md` § Debugging, `db/CLAUDE.md` § Debugging |

---

## Session history (AI-assisted work)

### 2026-06-07 — Session (claude-opus-4-8)
- Explored and documented repo: created `AGENTS.md` (root + api, ui, db) and `CLAUDE.md` pointers.
- Fixed Vercel build failure: added missing `ui/src/layouts/Layout.astro` and fixed `@` alias.
- Created `memory/changelog.md`, `memory/memory.md`, and changelog automation hook.
- Expanded per-service CLAUDE.md (api/ui/db → 800+ lines each): modules, routes, patterns, key surfaces, debugging.
- Added HH:MM timestamps to `.claude/hooks/update-changelog.sh` for same-day changelog ordering.
- Added linting config to `api/pyproject.toml` ([tool.ruff], [tool.black], [tool.isort], [tool.mypy]) + dev deps (ruff, black, isort, mypy, pytest, httpx, pytest-asyncio).
- Added ESLint to `ui/package.json` + `ui/.eslintrc.json` (Astro + TypeScript).
- Created `api/tests/` pytest skeleton: conftest.py (SQLite in-memory + DI override) + test_health.py, test_auth.py, test_users.py (9 tests covering auth contracts + CRUD).
- Updated `Makefile`: `make lint` (ruff+mypy), `make fmt` (black+isort), `make fmt-check` (exit 1 on drift), `make pytest`, `make lint-ui`.
