{
  "framework": "astro",
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "outputDirectory": "dist",
  "devCommand": "bun run dev",
  "ignoreCommand": "! git diff $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA --name-only -- ui/ | grep -v '\\.md$\\|\\.txt$' | grep -q ."
}
